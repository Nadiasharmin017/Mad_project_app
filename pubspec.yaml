name: philosophy_app
description: Philosophy-based daily inspiration app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.2
  intl: ^0.19.0
  shared_preferences: ^2.3.2

  sqflite: ^2.3.3+1
  path: ^1.9.0

  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.4

  cached_network_image: ^3.4.1

flutter:
  uses-material-design: true
  assets:
    - assets/locations.json
    - assets/quotes_fallback.json
